services:
  web:
    build: .
    expose:
      - 8080
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
      - type: bind
        source: /dev/shm/neon
        target: /dev/shm/neon